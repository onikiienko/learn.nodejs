var phrase;
exports.connect = function(){
  phrase = require('./ru')
}

exports.getPhrase = function(name){
  if(!phrase[name]){
    throw new Error("there is no phrase: " + name)
  } else {
    return phrase[name];
  }
}
